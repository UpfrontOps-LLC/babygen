# Claim Audit — 2026-06-28

Two evidence-driven auditors went through the repo **line by line**, verifying every
external-world / product / legal claim against **live official sources** (Stripe docs,
Replicate live schemas, Meta/Visa/Mastercard/Cloudflare official pages, and the actual
code). No training-data assumptions. Per [memory: evidence-driven-only].

**Headline:** the claims most likely to be hallucinated — the exotic Replicate model
names + params, and the Stripe restricted-business verdict — are **all real and correct.**
The genuine problems are (a) one wrong external number (already fixed) and (b) **the app
sells paid features the code never builds.**

## ❌ FALSE — external facts (FIXED)
| Claim | Where | Reality | Status |
|---|---|---|---|
| dispute thresholds "~0.65–0.9%" | LAUNCH_PLAN.md:33 | Visa VAMP merchant ≈2.2%→1.5% (Apr'26); Mastercard ECM 1.5%+100cb; Stripe internal watch ~0.75%. (0.5–0.7% is the *acquirer* band, not merchant.) Sources: ravelin.com VAMP, chargeflow ECM, docs.stripe.com/disputes/monitoring-programs | ✅ FIXED `cc76d74` |

## 🔧 INCONSISTENT-WITH-CODE — product/legal claims (PENDING — HARD GO-LIVE GATE)
**Do not enable real charges until each item is built or removed.**
| Claim | Where | Code reality | Fix |
|---|---|---|---|
| Deluxe "+ a giggle video 🎥" ($29) | checkout/route.ts:10, page.tsx:8 | `generate/route.ts` only makes 3 images (VARIANTS); never calls seedance | **BUILD** the seedance video post-pay (this is the new hero) |
| Ultimate "+ ages 5/10/18 + printable HD" ($49) | checkout/route.ts:11, page.tsx:9 | no age-progression code anywhere | BUILD or remove from sold tier |
| "add-ons charge to your card on file" | success/page.tsx:111 | OTO buttons only toggle local state + track(); no saved card / off-session PI | ✅ reworded (this commit) until built |
| "Photos not stored / deleted after generation" | page.tsx:143,159; layout.tsx:30; privacy:13,20 | `clearParents` empties an in-memory Map on the **success path only** — no failure/abandon deletion, no TTL; photos are also sent to Replicate | ✅ copy made accurate (this commit); **BUILD** reliable deletion (all-paths + TTL sweep) before go-live |

## ⚠️ UNSUPPORTED / IMPRECISE (PENDING — soften or measure)
| Claim | Where | Note | Fix |
|---|---|---|---|
| "HD results in ~30s" | page.tsx:160 | no telemetry; up to 90s polling per gen | measure or drop the number |
| "HD" / "3 HD photos" | page.tsx:78,160; checkout:9-11 | code requests `resolution:"1K"` (~1024px) | bump to 2K or soften "HD" |
| "fits KV's 25 MB value limit" | DEPLOY_STRIPE.md:59 | Cloudflare KV = 25 **MiB**; base64 inflates ~33% | say MiB; prefer R2 for blobs |
| "NOT in any restricted category" | LAUNCH_PLAN.md:24-28 | "not explicitly listed" is fair; "any" overstates; still under Stripe AUP | soften |
| README "Deploy on Vercel" | README.md | stale create-next-app boilerplate; we target Cloudflare | replace |
| marketing doc items (12) | LAUNCH_PLAN/UPSELLS/ad-copy | COGS estimates, BIPA "TX/WA too", before/after-as-blanket, reviewer-behavior guesses | ✅ FIXED `cc76d74` |

## ✅ VERIFIED — checked out, no change needed
- **Replicate (live authed schemas):** `google/nano-banana-pro` (`resolution` enum `['1K','2K','4K']`, `safety_filter_level` enum incl. `block_only_high`, `image_input` array ≤14, `output_format` `['jpg','png']`) and `bytedance/seedance-1-lite` (`camera_fixed` bool, `duration` 4–12, `resolution` `['480p','720p','1080p']`). All params used are valid.
- **Stripe:** raw-body webhook verification (`req.text()`+`constructEvent`), fail-closed on bad sig, `payment_status` gate, `new Stripe(key)` default apiVersion, `generateTestHeaderString` — all real (stripe@22.3.0 installed).
- **Stripe restricted-businesses:** AI face/baby generator not prohibited/restricted (stripe.com/restricted-businesses). IL BIPA $1k/$5k per violation.
- **Versions:** next 16.2.9, react 19.2.4, stripe 22.3.0 — installed.

## Counts
FALSE: 1 (fixed) · INCONSISTENT-WITH-CODE: 7 (2 reworded now, 2 builds gated) · UNSUPPORTED/IMPRECISE: 9 (12 marketing already fixed) · VERIFIED: full Replicate + Stripe surface.
